# Prometheus 抓取配置：app 的 /metrics（compose 内网服务名）
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: hr-agent
    metrics_path: /metrics
    static_configs:
      - targets: ["app:8000"]
        labels:
          service: hr-agent
