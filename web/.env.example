# 后端 API 基础路径（经 Vite dev proxy 时保持 /api 即可）
VITE_API_BASE=/api

# Mock 演示模式：true 时不依赖后端，前端内置流式响应与模拟审批流
VITE_MOCK=false

# dev proxy 目标（ vite.config.ts 读取，仅开发期生效）
# VITE_PROXY_TARGET=http://localhost:8000
